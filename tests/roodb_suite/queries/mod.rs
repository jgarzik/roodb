//! Query integration tests.
//!
//! Tests for SELECT statements including basic queries, WHERE clauses,
//! aggregates, grouping, ordering, and JOINs.

mod aggregates;
mod date_functions;
mod grouping;
mod joins;
mod ordering;
mod procedures;
mod select_basic;
mod subqueries;
mod user_variables;
mod where_clause;
